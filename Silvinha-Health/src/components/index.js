import { View, Text } from 'react-native';

export default function Title(){
    return (
    <View style={styles.container}>
        <Textstyle> style={styles.title}Silvinha Health App!</Textstyle>
        <StatusBar style="auto" />
      </View>
    );
}